apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: spesidaler-db-policy-dev
  namespace: tbd
spec:
  egress:
    - to:
        - ipBlock:
            cidr: 34.88.147.77/32
  podSelector:
    matchLabels:
      db_policy_user: spesidalerbruker
  policyTypes:
    - Egress
